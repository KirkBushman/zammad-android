<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        package="com.kirkbushman.sampleapp">

    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
            android:name=".SampleApplication"
            android:allowBackup="false"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            android:networkSecurityConfig="@xml/network_security_config"

            tools:targetApi="n"
            tools:ignore="GoogleAppIndexingWarning">

        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity" />
        <activity android:name=".activities.MeActivity" />
        <activity android:name=".activities.TicketActivity" />
        <activity android:name=".activities.TicketsActivity" />
        <activity android:name=".activities.TicketCreateActivity" />
        <activity android:name=".activities.TicketUpdateActivity" />
        <activity android:name=".activities.ActivitySearch" />
        <activity android:name=".activities.AttachmentActivity" />
        <activity android:name=".activities.AttachmentsActivity" />
        <activity android:name=".activities.UserActivity" />
        <activity android:name=".activities.UserUpdateActivity" />
        <activity android:name=".activities.UsersActivity" />
        <activity android:name=".activities.GroupActivity" />
        <activity android:name=".activities.GroupUpdateActivity" />
        <activity android:name=".activities.GroupsActivity" />
        <activity android:name=".activities.RoleActivity" />
        <activity android:name=".activities.RolesActivity" />
        <activity android:name=".activities.TagsActivity" />
        <activity android:name=".activities.OverviewActivity" />
        <activity android:name=".activities.OverviewsActivity" />
        <activity android:name=".activities.OrganizationActivity" />
        <activity android:name=".activities.OrganizationUpdateActivity" />
        <activity android:name=".activities.OrganizationsActivity" />
        <activity android:name=".activities.PriorityActivity" />
        <activity android:name=".activities.PriorityUpdateActivity" />
        <activity android:name=".activities.PrioritiesActivity" />
        <activity android:name=".activities.StateActivity" />
        <activity android:name=".activities.StateUpdateActivity" />
        <activity android:name=".activities.StatesActivity" />
        <activity android:name=".activities.NotificationsActivity" />
        <activity android:name=".activities.ArticleActivity" />
        <activity android:name=".activities.ArticlesActivity" />
        <activity android:name=".activities.ArticleCreateActivity" />

        <meta-data android:name="android.max_aspect" android:value="2.1" />

    </application>

</manifest>